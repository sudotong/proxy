{
  "private": true,
  "name": "now-proxy",
  "version": "1.0.0",
  "description": "Deploy a reverse proxy as a now.sh instance",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": "6.x.x"
  },
  "dependencies": {
    "http-proxy": "^1.14.0"
  }
}
